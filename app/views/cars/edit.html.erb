<h1>Editing car</h1>

<%= render "form", car: @car %>

<br>

<div>
  <%= link_to "Show this car", @car, class:"btn btn-secondary" %> 
  <br><br>
  <%= button_to 'Delete', @car, method: :delete, data: { turbo_confrim: 'Are you sure?' }, class: "btn btn-danger btn-sm" %>
  <br>
  <%= link_to "Back to cars", cars_path, class:"btn btn-dark" %>
</div>
